services:
  db:
    image: postgres:15
    container_name: fernando_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  backe_end:
    build: .
    restart: always
    container_name: fernando_backend
    ports:
      - "5012:5000"
    env_file:
    - path: .env
      required: false

volumes:
  db_data: